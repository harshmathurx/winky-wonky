// AudioSynth moved to `@winkywonky/core` in Phase 5 (see docs/AUDIT.md Part 2) —
// this file is now a stable re-export so every component's
// `import { AudioSynth } from './audioSynth.js'` keeps working unchanged.
export { AudioSynth } from '@winkywonky/core';
