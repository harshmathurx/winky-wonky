---
"@winkywonky/core": patch
---

Renamed the package scope from `@winky/core` to `@winkywonky/core` to match the npm organization actually created for this project (https://www.npmjs.com/org/winkywonky). Nothing was ever published under the old name, so this isn't a breaking change for any real consumer — just correcting the name before the first release.
